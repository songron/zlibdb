#!/usr/bin/env python

from distutils.core import setup

setup(name='zlibdb',
      version='0.1',
      description='Key-Value database based on zlib and sqlite3',
      author='Song',
      py_modules=['zlibdb']
      )
